<?php

function ao_task78_test_cache_user_login($account) {
  if (!$account->getLastAccessedTime()) {
    $cid = $account->getAccountName();
    \Drupal::cache()->set('user', $cid);
    drupal_set_message(t('Ваш первый вход на сайт. Приветствуем!'));
  }
}